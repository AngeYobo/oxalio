name: Maven CI (api-gateway)

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Récupère le code source
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Installe le JDK + configure le cache Maven
      - name: Set up Temurin JDK 17 with Maven cache
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'maven'

      # 3. Cache .m2 pour accélérer les builds
      - name: Cache local Maven repository
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: maven-${{ github.run_id }}
          restore-keys: |
            maven-

      # 4. Build backend complet (évite les problèmes de dépendances locales)
      - name: Build all backend modules
        run: mvn -ntp clean install
        working-directory: backend

      # 5. Exécute les tests sur api-gateway uniquement
      - name: Run tests for api-gateway
        run: mvn -ntp test -pl api-gateway
        working-directory: backend

      # 6. Archive les rapports de tests pour consultation
      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: junit-reports
          path: |
            backend/api-gateway/target/surefire-reports/**
            backend/api-gateway/target/failsafe-reports/**
          if-no-files-found: ignore
